# SUI Combats v5.1 (13-slot) — Fresh Cut-over QA Checklist

> **State when you wake up:** v5.1 13-slot contracts LIVE on testnet (package `0x95c231e5909678609202b5a976c575366280222cdb703e4a7a097043bd712c69`). Servers running on v5.1 env. Mr_Boss + Sx wallets each have 13 items (one per slot) ready to equip. v5.0 chain state is frozen on chain (unusable post-cutover; this is by Sui semantics).
>
> **The cut-over was destructive and complete:** v5.0 in-memory characters wiped (`.local-state/characters.json` cleared), new env files in place, fresh package. Players will need to mint NEW characters on first connect — the v5.0 Characters from the prior session are unusable on the v5.1 chain.

---

## What you should see on first open

Open http://localhost:3000 in a browser as Mr_Boss (or Sx). Expected:

| Surface | Expected |
|---|---|
| Wallet connect | Slush / browser wallet / Enoki sign-in all available |
| Character status | "No character — create one" (v5.0 chars frozen, v5.1 starts empty) |
| Inventory before character mint | If you query the wallet via Sui explorer, you should see 13 Items in the wallet's owned-objects. They won't appear in the in-game inventory until a character exists. |
| Tavern | Empty player list (no online; fresh state) |
| Marketplace | "0 listings" — fresh state |
| Hall of Fame | Empty (no fights yet) |

---

## QA — 13-slot smoke test (run end-to-end)

The goal is to walk every new chain path and confirm the 3 new slots equip, the bonuses count in combat math, and the loot saves via the save_loadout PTB.

### Step 1 — Character mint with chain registry guard

- [ ] As Mr_Boss, click **Create Character** → 5/5/5/5 stat allocation, name "MrBoss".
- [ ] Confirm wallet popup shows `character::create_character` with the CharacterRegistry as an argument.
- [ ] Sign. Confirm character mints successfully.
- [ ] **Test the registry guard:** Try to mint a SECOND character on the same wallet. Expected: PTB aborts with `EWalletAlreadyHasCharacter (code 6)`. The frontend should surface a friendly message; if it shows the raw abort, that's a UX polish item but the chain enforcement is the win.
- [ ] Repeat for Sx (4/6/5/5 stat allocation, name "ShakaLiX" — evasion/dex build).

### Step 2 — Inventory population

- [ ] On Mr_Boss's character page, **all 13 slot tiles should be visible** (weapon, offhand, helmet, chest, gloves, boots, belt, ring_1, ring_2, necklace, pants, bracelets, pauldrons).
- [ ] Inventory should show 13 items distributed by mint script:
  - **Mr_Boss kit:** Iron Shortsword, Buckler, Iron Cap, Leather Vest, Leather Gloves, Leather Boots, Studded Belt, Iron Signet, Bone Ring, Wolf Tooth Pendant, Iron Greaves, Spiked Bracelets, Iron Pauldrons.
- [ ] Sx kit: Twin Stiletto + Twin Stiletto B (dual-wield-capable), Hunter Hood, Shadow Vest, Shadow Gloves, Whisperwind Boots, Quicksilver Belt, Moonstone Ring, Fox Pearl, Cat-eye Amulet, Quicksilver Trousers, Silken Wraps, Shadowstep Mantle.

> **Inventory has duplicates** for both wallets — the first mint run partially succeeded before I added gas-coin retry, so a few items minted twice. Harmless — testnet is treating it as overflow inventory.

### Step 3 — Equip every slot via Save Loadout PTB

- [ ] As Mr_Boss, drag/click items into every slot. Verify the dirty-slot indicator fires on each.
- [ ] Click **Save Loadout** → wallet popup shows a PTB with **~14 moveCall instructions** (13 equip_* + 1 save_loadout). Sign.
- [ ] Confirm all 13 slots are now filled.
- [ ] **Verify combat stats include new slots:** Open the stats panel; verify the HP / armor / attack / defense / crit / evasion totals reflect bonuses from PANTS, BRACELETS, and PAULDRONS specifically. Iron Greaves alone gives +5 HP +3 armor +2 endurance.
- [ ] As Sx, repeat: equip everything via Save Loadout. Sx has 14 items (two Twin Stilettos for dual-wield + 12 armor pieces).
  - [ ] **Dual-wield path:** equip Twin Stiletto into weapon, Twin Stiletto B into offhand. Confirm chain accepts (both are `slot_type=mainhand`).

### Step 4 — Chain-side enforcement smoke

- [ ] Verify the slot_type rules fire (these are negative tests — abort expected):
  - [ ] **Two-handed weapons:** none in the catalog (Mr_Boss + Sx both got mainhand-only weapons). To test, mint a `slot_type=2` weapon via the catalog script with a small edit, or skip — Move tests cover the path.
  - [ ] **EWeaponSlotTypeInvalid (item.move:7):** mint a weapon with `slot_type=offhand` → contract aborts. (Move unit test covers this; live walk optional.)

### Step 5 — Wager flow (registry-gated)

- [ ] As Mr_Boss, **Create Wager** at 0.1 SUI. Wallet popup shows the OpenWagerRegistry threaded into `arena::create_wager`. Sign.
- [ ] As Sx, accept the wager. Wallet popup shows OpenWagerRegistry threaded into `arena::accept_wager`. Sign.
- [ ] Confirm the chain probe + finality wait fires on the server (look for `[handleWagerAccepted] tx-finality confirmed digest=… proceeding to status probe` in `/tmp/server.log`).
- [ ] Fight to a winner. Confirm settlement: winner gets 95% (`settle_wager`), TREASURY gets 5%. Both characters' on-chain W/L updates.

### Step 6 — Mutual-KO (`settle_tie`) live verification

- [ ] As Mr_Boss + Sx, create + accept a 0.1 SUI wager.
- [ ] In the fight, BOTH attack the same zone every turn (e.g. both pick head + block stomach+belt — guarantees max damage through unblocked head). After 5-6 turns one of you should mutual-KO.
- [ ] If mutual-KO happens, expected:
  - [ ] Server log: `[Wager] Draw — settling tie on chain for 0x…`
  - [ ] Server log: `[Wager] Settle-tie complete. Tx: 0x…`
  - [ ] Both wallets receive full stake refund (0.1 SUI each — no platform fee on draws).
  - [ ] Both characters' on-chain `draws: u32` counter increments by 1.
  - [ ] UI: both clients still show "Defeat / You Lose" — Draw modal frontend deferred to a focused frontend session.

### Step 7 — Duplicate-wager guard

- [ ] As Mr_Boss, create a wager AND don't cancel. Try to **Create another wager** without cancelling first.
- [ ] Expected: PTB aborts with `EAlreadyHasOpenWager (code 11)`. Frontend should refuse (chain guard).
- [ ] Cancel the wager. Confirm registry clears (you can now create another).

### Step 8 — Kiosk registry guard

- [ ] As Mr_Boss, **Create Kiosk** (or do nothing — the UI's createKiosk action triggers it).
- [ ] First-click: wallet popup shows `marketplace::create_or_get_player_kiosk` with KioskRegistry. Sign. Kiosk created.
- [ ] Second-click: should be idempotent — returns existing kiosk_id. No new Kiosk should be created on chain.
- [ ] Verify in the chain explorer: Mr_Boss should own **exactly one** `KioskOwnerCap` after multiple clicks.

### Step 9 — List + buy with new policy

- [ ] As Mr_Boss, list one of his items (e.g. Bone Ring) for 0.05 SUI.
- [ ] As Sx, buy it. Verify 2.5% royalty math (Sx pays 0.05 + 0.00125 royalty floor → 1000 MIST minimum applies, so 0.001 SUI royalty + 0.05 price = 0.051 total cost).
- [ ] Mr_Boss withdraws kiosk profits.

### Step 10 — XP curve + level-up

- [ ] After 5-10 wager/ranked fights, watch for the level-up modal (~3000 XP to L7 from L1).
- [ ] When it fires: should pop with "Level Up x{N}!" celebration + "Allocate Stat Points" CTA.

---

## Deployment artefacts (read-only references)

```
v5.1 13-slot package:    0x95c231e5909678609202b5a976c575366280222cdb703e4a7a097043bd712c69
AdminCap:                0x7796675da7a290085c1b1ed812ed344abff69bd29544d7d10cc5fa28a383576a
UpgradeCap:              0x6216b0c33d01a9976d034b712985eb8f3d1c7886972e591cd470ea2e2e7f07d1
Publisher:               0x3d9e48b7d647f6d8bd1b57622b684f4372a148386c3c6c3f53f7cc17fae4ed6c
TransferPolicy<Item>:    0x6e5319160d84bbc3492725bbbaab2004629886e664aad928f730c83e80cb0654
TransferPolicyCap<Item>: 0x5464653700d6bf22ea6ac4e4da7368520889fcc024f0e97eee1169652fe5f0d8

CharacterRegistry:       0x6c4fdf3cff4617903fe8bfbd24a9e81f4e3e15f4efbef614ecea1d6056ab2a1a
OpenWagerRegistry:       0x3a7f198458af69490d58ac03460be9c123b8d90160d04fdb828d2c3e67d36b7c
KioskRegistry:           0x4994aa3eae7d6922c7db466c69b1e9d135aa23837be26905eb0179f43ee5d17b

TREASURY:                0x975f1b348625cdb4f277efaefda1d644b17a4ffd97223892d93e93277fe19d4d

Mr_Boss:                 0xf669789c0e6d30627e8480b5886721d608d796277aab0664cfa84b2c04590f33
Sx:                      0x03c33df0c97d4dfb3792d340bbf83891e2a20d653155874fd37a350ad443985f
```

Sui explorer (testnet): https://testnet.suivision.xyz/object/0x95c231e5909678609202b5a976c575366280222cdb703e4a7a097043bd712c69

---

## Server / frontend state

| | |
|---|---|
| Branch | `feature/v5.1-contracts` @ HEAD `55583cf` (this commit) |
| Backend | running on `:3001`, env switched to v5.1 13-slot |
| Frontend | running on `:3000`, env switched to v5.1 13-slot |
| v5.0 env backups | `server/.env.v5.0-backup` + `frontend/.env.local.v5.0-backup` |
| v5.0 character state | wiped — `/tmp/local-state.v5.0-backup-…` retains the file if you ever need it |
| Move tests | 35 → 71 PASS (+36 since v5.0) |
| Server tsc | clean |
| Frontend tsc | clean |
| Treasury remaining | ~2.95 SUI (post-publish + post-mint of 37 items including the partial first run) |

---

## What's still NOT in v5.1 (still v5.2 deferred per the audit)

These are intentional defers — NOT bugs.

- **sui::random** for loot rolls (trust improvement)
- **respec_character** with 5 SUI sink + 24h cooldown
- **settle_wager_attested** with dual-signature settlement (closes TREASURY-trust assumption)
- **Confirm-modal gate** for every signAndExecuteTransaction (HARD mainnet blocker per MAINNET_PREP §D)
- **Settlement retry / journal** (audit's #1 mainnet risk)
- **WS Zod validation + rate-limiter middleware**
- **Frontend Draw modal + W/L/D counter** (server records draws on chain; UI still shows "Defeat")
- **Display objects** (`display::new<Character>` + `display::new<Item>` via Publisher) — NFTs visible in wallet but without Mysten Display schema; recommend setting up before more mints.
- **StatBonuses shape unification** (server vs frontend mismatch drops 4 stat fields silently)
- **Empty catch sweep**

---

## Operational notes

- **Display setup is recommended next** if you want NFTs to render with proper schema in Sui wallet extensions. Call `display::new<Character>` and `display::new<Item>` via the Publisher (`0x3d9e48b7…ed6c`). Mirror v5.0 schema (image_url template, name, description).
- **v5.0 (10-slot v5.1)** packages `0xa7dc2dab…` and `0x7853412f…` remain on chain. They're callable but disconnected from the runtime. Player wallets that held v5.0 character NFTs still own them on chain; they're just unusable in the v5.1 game.
- **`Published.toml`** now points at the 13-slot package. Future upgrades (if ever) work through the `UpgradeCap` `0x6216b0c3…`.
- **Backup files**: `server/.env.v5.0-backup`, `frontend/.env.local.v5.0-backup`, `/tmp/Published.toml.v5.1-13slot-pre-backup`, `/tmp/local-state.v5.0-backup-*`. All safe to delete after a clean QA pass.
- **Mint script reusable**: `scripts/mint-v5.1-13slot-catalog.ts` can be edited + re-run any time TREASURY needs to populate more test inventory.

---

*Generated 2026-05-28 PM autonomous run. Branch `feature/v5.1-contracts`. See `docs/V5.1_OVERNIGHT_LOG_2026-05-28.md` for the per-phase journal of both the overnight contract ship and this 13-slot follow-up.*
